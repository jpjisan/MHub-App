module.exports = {
  // ...existing config
  theme: {
    extend: {
      animation: {
        shimmer: 'shimmer 2s linear infinite',
        ratingPulse: 'ratingPulse 0.6s ease-in-out'
      }
    }
  }
}